@model AimsSchoolMaster.Models.StudentViewModel
@{
    ViewBag.Title = "Student Information";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("ViewStudent", "Portal", FormMethod.Post, new { enctype = "multipart/form-data", @class = "form-horizontal", role = "form" }))
{

    @Html.AntiForgeryToken()
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="container">
        <div class="form-group">
            @Html.LabelFor(m => m.StudentName, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.Label(Model.StudentName, new { @class = "col-md-2 control-label" })
                @Html.HiddenFor(model => model.StudentName)
            </div>
        </div>
        @*<br />
        <hr />
        <h4>Assignments</h4>
        <div class="form-group">
            Assign new Assignment:
            <input type="file" name="postedFile" />
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" class="btn btn-default" value="Upload File" />
            </div>
        </div>*@
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}